"""Shim tests. Stdlib ``unittest`` only — see test_cli.py.

Present so ``python -m unittest discover -s tests -t .`` can import this directory
as a package while resolving ``mono_control_shim`` from the repo root.
"""
